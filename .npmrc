# NPM Configuration for @crego/ui-kit
# This file ensures proper npm behavior for publishing

# Ensure we're using the public npm registry
registry=https://registry.npmjs.org/


# Ensure proper access for scoped packages
@crego:registry=https://registry.npmjs.org/
